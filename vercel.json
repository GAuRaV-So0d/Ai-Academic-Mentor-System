{
  "version": 2,
  "buildCommand": "cd backend && npm install && cd ..",
  "public": "frontend",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(?!api).*",
      "dest": "/index.html"
    }
  ]
}
